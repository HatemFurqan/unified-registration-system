# دليل شامل: Docker للمشروع - Unified Registration System

## 📚 الفهرس
1. [نظرة عامة](#نظرة-عامة)
2. [الملفات ووظائفها](#الملفات-ووظائفها)
3. [علاقة الملفات ببعضها](#علاقة-الملفات-ببعضها)
4. [آلية العمل خطوة بخطوة](#آلية-العمل-خطوة-بخطوة)
5. [التسلسل الزمني](#التسلسل-الزمني)

---

## 🎯 نظرة عامة

### ما هو Docker؟
Docker هو نظام لتشغيل التطبيقات في حاويات (Containers) منعزلة. تخيل أن لديك صندوقاً يحتوي على كل شيء يحتاجه التطبيق للعمل (PHP، قاعدة البيانات، الملفات، إلخ).

### لماذا نستخدم Docker؟
- ✅ **سهولة النشر**: نفس البيئة في التطوير والإنتاج
- ✅ **العزل**: كل مشروع في حاوية منفصلة
- ✅ **السرعة**: أسرع من الـ Virtual Machines
- ✅ **التوافق**: يعمل على أي سيرفر

---

## 📁 الملفات ووظائفها

### 1. `Dockerfile` - وصفة بناء الحاوية

**ما هو؟**
- ملف يصف كيفية بناء صورة Docker
- مثل "وصفة طبخ" للحاوية

**ماذا يفعل؟**
```
1. يبدأ من صورة PHP جاهزة (php:8.0-fpm)
2. يثبت الأدوات المطلوبة (git, curl, إلخ)
3. يثبت إضافات PHP (pdo_mysql, mbstring, إلخ)
4. يثبت Composer (أداة إدارة مكتبات PHP)
5. ينشئ مستخدماً (hatem) بصلاحيات محددة
6. ينسخ ملفات المشروع إلى الحاوية
```

**مثال بسيط:**
```
من: صورة PHP فارغة
إلى: صورة جاهزة تحتوي على:
     - PHP 8.0
     - Composer
     - جميع المكتبات المطلوبة
     - ملفات المشروع
```

**العلاقة:**
- يُستخدم من قبل `docker-compose.yml` عند البناء
- يحتاج معلومات من `docker-compose.yml` (user, uid, gid)

---

### 2. `docker-compose.yml` - مدير الحاويات

**ما هو؟**
- ملف يصف جميع الحاويات المطلوبة وكيفية ربطها
- مثل "مدير المشروع" الذي ينظم كل شيء

**ماذا يفعل؟**
```
1. يبني حاوية التطبيق (app) من Dockerfile
2. يشغل حاوية Nginx (webserver)
3. يربط الحاويات ببعضها
4. يحدد المسارات والصلاحيات
5. يربط الملفات من السيرفر إلى الحاوية
```

**الحاويات التي ينشئها:**
- **unified-registration-app**: حاوية PHP-FPM (التطبيق)
- **unified-registration-server**: حاوية Nginx (الخادم)

**العلاقة:**
- يستخدم `Dockerfile` لبناء حاوية app
- يستخدم `nginx-app.conf` لإعدادات Nginx
- يستخدم `local.ini` لإعدادات PHP
- يربط ملفات السجلات

---

### 3. `nginx-app.conf` - إعدادات خادم الويب

**ما هو؟**
- ملف إعدادات Nginx (خادم الويب)
- يحدد كيف يتعامل Nginx مع الطلبات

**ماذا يفعل؟**
```
1. يستمع على البورت 80
2. عندما يأتي طلب على /unified-registration/
3. يرسل الطلب إلى حاوية app (PHP-FPM)
4. يعيد النتيجة للمستخدم
```

**مثال بسيط:**
```
المستخدم يطلب: https://furqanshop.com/unified-registration/
↓
Nginx يستقبل الطلب
↓
يرسله إلى unified-registration-app:9000 (PHP-FPM)
↓
PHP يعالج الطلب
↓
يرجع النتيجة إلى Nginx
↓
Nginx يرسلها للمستخدم
```

**العلاقة:**
- يُستخدم من قبل حاوية nginx في `docker-compose.yml`
- يحتاج حاوية app لتكون جاهزة (depends_on)

---

### 4. `local.ini` - إعدادات PHP

**ما هو؟**
- ملف إعدادات PHP
- يحدد قيود PHP (مثل حجم الملفات)

**ماذا يفعل؟**
```
يحدد:
- upload_max_filesize=40M (أقصى حجم ملف يمكن رفعه)
- post_max_size=40M (أقصى حجم بيانات يمكن إرسالها)
```

**العلاقة:**
- يُستخدم من قبل حاوية app في `docker-compose.yml`
- يُربط داخل الحاوية في `/usr/local/etc/php/conf.d/local.ini`

---

### 5. `git_pull.sh` - سكريبت التحديث اليدوي

**ما هو؟**
- سكريبت Bash لتحديث المشروع يدوياً
- يُستخدم عند التحديثات الكبيرة

**ماذا يفعل؟**
```
1. يتحقق من أنك root (sudo)
2. يعطي صلاحيات كاملة للمشروع
3. يصلح إعدادات Git
4. يسحب التحديثات من Git
5. يثبت المكتبات الجديدة (composer install)
```

**متى يُستخدم؟**
- عند التحديثات الكبيرة
- عند وجود مشاكل في التحديث التلقائي
- للصيانة اليدوية

**العلاقة:**
- يعمل على السيرفر مباشرة
- يحتاج حاوية app لتكون جاهزة
- بديل لـ GitHub Actions عند الحاجة

---

### 6. `.github/workflows/deploy.yml` - التحديث التلقائي

**ما هو؟**
- ملف GitHub Actions
- يحدث المشروع تلقائياً عند push إلى Git

**ماذا يفعل؟**
```
عند push إلى GitHub:
1. يتصل بالسيرفر عبر SSH
2. ينتقل إلى مجلد المشروع
3. يشغل الحاويات (إذا لم تكن تعمل)
4. يصلح الصلاحيات
5. يسحب التحديثات من Git
6. يثبت المكتبات الجديدة
```

**متى يعمل؟**
- تلقائياً عند push إلى branch `main`
- لا يحتاج تدخل يدوي

**العلاقة:**
- يعمل من GitHub
- يحتاج إعدادات SSH في GitHub Secrets
- يحتاج حاويات جاهزة على السيرفر

---

## 🔗 علاقة الملفات ببعضها

### المخطط العام:

```
┌─────────────────────────────────────────────────────────┐
│                    GitHub Repository                    │
│  (الكود المصدري + .github/workflows/deploy.yml)        │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ (عند push)
                     ↓
┌─────────────────────────────────────────────────────────┐
│              GitHub Actions (deploy.yml)                 │
│  - يتصل بالسيرفر                                        │
│  - يشغل docker compose                                  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────┐
│              السيرفر (Hostinger VPS)                    │
│                                                          │
│  ┌──────────────────────────────────────────────┐      │
│  │  docker-compose.yml                          │      │
│  │  ├─ يقرأ Dockerfile                         │      │
│  │  ├─ يبني unified-registration-app           │      │
│  │  ├─ يشغل unified-registration-server        │      │
│  │  ├─ يربط nginx-app.conf                     │      │
│  │  └─ يربط local.ini                          │      │
│  └──────────────────────────────────────────────┘      │
│                                                          │
│  ┌──────────────────────────────────────────────┐      │
│  │  Docker Containers (الحاويات)                │      │
│  │                                               │      │
│  │  ┌─────────────────────────────────────┐    │      │
│  │  │ unified-registration-app            │    │      │
│  │  │ (PHP-FPM)                           │    │      │
│  │  │ - يعمل PHP                          │    │      │
│  │  │ - يستخدم local.ini                  │    │      │
│  │  │ - يحتوي على المشروع                 │    │      │
│  │  └─────────────────────────────────────┘    │      │
│  │              ↕ (يتواصل عبر البورت 9000)     │      │
│  │  ┌─────────────────────────────────────┐    │      │
│  │  │ unified-registration-server         │    │      │
│  │  │ (Nginx)                             │    │      │
│  │  │ - يستقبل الطلبات                    │    │      │
│  │  │ - يستخدم nginx-app.conf             │    │      │
│  │  │ - يرسل PHP requests إلى app         │    │      │
│  │  └─────────────────────────────────────┘    │      │
│  └──────────────────────────────────────────────┘      │
│                                                          │
│  ┌──────────────────────────────────────────────┐      │
│  │  git_pull.sh (سكريبت يدوي)                  │      │
│  │  - يمكن تشغيله يدوياً                       │      │
│  │  - يحدث المشروع                             │      │
│  └──────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────┘
```

### العلاقات التفصيلية:

#### 1. **Dockerfile ↔ docker-compose.yml**
```
docker-compose.yml يقول: "استخدم Dockerfile لبناء الحاوية"
Dockerfile يقول: "هذه هي التعليمات للبناء"
```

#### 2. **docker-compose.yml ↔ nginx-app.conf**
```
docker-compose.yml يربط nginx-app.conf داخل حاوية nginx
nginx-app.conf يحدد كيف يتعامل nginx مع الطلبات
```

#### 3. **docker-compose.yml ↔ local.ini**
```
docker-compose.yml يربط local.ini داخل حاوية app
local.ini يحدد إعدادات PHP
```

#### 4. **deploy.yml ↔ docker-compose.yml**
```
deploy.yml يشغل docker compose up -d
docker-compose.yml يبني ويشغل الحاويات
```

#### 5. **git_pull.sh ↔ docker exec**
```
git_pull.sh يستخدم docker exec للدخول إلى الحاوية
docker exec ينفذ الأوامر داخل الحاوية
```

---

## ⚙️ آلية العمل خطوة بخطوة

### المرحلة 1: البناء (Build)

```
1. المطور يكتب: docker compose build
   ↓
2. docker-compose.yml يقرأ Dockerfile
   ↓
3. Dockerfile يبدأ من صورة PHP جاهزة
   ↓
4. Dockerfile يثبت الأدوات (git, curl, إلخ)
   ↓
5. Dockerfile يثبت إضافات PHP
   ↓
6. Dockerfile يثبت Composer
   ↓
7. Dockerfile ينشئ مستخدماً (hatem)
   ↓
8. Dockerfile ينسخ ملفات المشروع
   ↓
9. النتيجة: صورة جاهزة باسم unified-registration-image
```

### المرحلة 2: التشغيل (Run)

```
1. المطور يكتب: docker compose up -d
   ↓
2. docker-compose.yml يقرأ الإعدادات
   ↓
3. ينشئ حاوية app من الصورة
   ↓
4. ينشئ حاوية nginx من صورة nginx
   ↓
5. يربط nginx-app.conf داخل nginx
   ↓
6. يربط local.ini داخل app
   ↓
7. يربط ملفات المشروع (./:/var/www)
   ↓
8. يربط الحاويات ببعضها (network: proxy)
   ↓
9. النتيجة: حاويتان تعملان معاً
```

### المرحلة 3: معالجة الطلب

```
المستخدم يفتح: https://furqanshop.com/unified-registration/
   ↓
Traefik (reverse proxy) يستقبل الطلب
   ↓
Traefik يرسل الطلب إلى unified-registration-server (nginx)
   ↓
nginx يقرأ nginx-app.conf
   ↓
nginx يرى: location /unified-registration/
   ↓
nginx يرسل الطلب إلى unified-registration-app:9000 (PHP-FPM)
   ↓
PHP-FPM (في app) يعالج الطلب
   ↓
PHP-FPM يقرأ local.ini (إعدادات PHP)
   ↓
PHP-FPM ينفذ كود Laravel
   ↓
Laravel يعيد النتيجة
   ↓
PHP-FPM يرسل النتيجة إلى nginx
   ↓
nginx يرسل النتيجة إلى Traefik
   ↓
Traefik يرسل النتيجة للمستخدم
   ↓
المستخدم يرى الصفحة!
```

### المرحلة 4: التحديث التلقائي

```
المطور يكتب: git push
   ↓
GitHub يستقبل التحديثات
   ↓
GitHub Actions يقرأ .github/workflows/deploy.yml
   ↓
deploy.yml يتصل بالسيرفر عبر SSH
   ↓
deploy.yml ينتقل إلى مجلد المشروع
   ↓
deploy.yml يشغل: docker compose up -d (للتأكد من التشغيل)
   ↓
deploy.yml يصلح الصلاحيات
   ↓
deploy.yml ينفذ: git pull داخل الحاوية
   ↓
deploy.yml ينفذ: composer install
   ↓
النهاية: المشروع محدث!
```

---

## 📊 التسلسل الزمني

### عند النشر الأول:

```
الوقت | العملية
──────┼────────────────────────────────────────────
T0    | رفع الملفات إلى السيرفر
T1    | تعديل .env
T2    | docker compose build (بناء الصورة)
T3    | docker compose up -d (تشغيل الحاويات)
T4    | composer install (تثبيت المكتبات)
T5    | php artisan migrate (إنشاء الجداول)
T6    | php artisan key:generate (توليد المفتاح)
T7    | المشروع جاهز!
```

### عند التحديث (GitHub Actions):

```
الوقت | العملية
──────┼────────────────────────────────────────────
T0    | git push إلى GitHub
T1    | GitHub Actions يبدأ
T2    | الاتصال بالسيرفر (SSH)
T3    | docker compose up -d (تشغيل الحاويات)
T4    | إصلاح الصلاحيات
T5    | git pull (سحب التحديثات)
T6    | composer install (تثبيت المكتبات الجديدة)
T7    | التحديث مكتمل!
```

---

## 🎓 شرح مبسط (لطالب سنة أولى)

### تخيل أنك تريد بناء بيت:

#### 1. **Dockerfile** = مخطط البناء
```
- يخبر العمال (Docker) كيف يبنون البيت
- يقول: "ابدأ من أساس جاهز (PHP)"
- يقول: "أضف الأدوات (git, curl)"
- يقول: "أضف المكتبات (pdo_mysql)"
- يقول: "ضع الملفات هنا"
```

#### 2. **docker-compose.yml** = مدير المشروع
```
- يقول: "ابنِ البيت حسب Dockerfile"
- يقول: "أنشئ أيضاً خادم ويب (nginx)"
- يقول: "اربط البيت بالخادم"
- يقول: "استخدم هذه الإعدادات"
```

#### 3. **nginx-app.conf** = قواعد الاستقبال
```
- يقول للخادم: "عندما يأتي شخص على /unified-registration/"
- يقول: "أرسله إلى البيت (app)"
- يقول: "خذ النتيجة وأرسلها للشخص"
```

#### 4. **local.ini** = قواعد البيت
```
- يقول: "لا تقبل ملفات أكبر من 40MB"
- يقول: "لا تقبل بيانات أكبر من 40MB"
```

#### 5. **git_pull.sh** = عامل الصيانة
```
- يقول: "أعطني صلاحيات كاملة"
- يقول: "اجلب التحديثات الجديدة"
- يقول: "ثبت المكتبات الجديدة"
```

#### 6. **deploy.yml** = روبوت التحديث
```
- يقول: "عندما يحدث شيء في GitHub"
- يقول: "اتصل بالسيرفر"
- يقول: "حدث المشروع تلقائياً"
```

---

## 🔄 دورة الحياة الكاملة

### 1. **البناء (Build)**
```
Dockerfile → صورة Docker → جاهزة للاستخدام
```

### 2. **التشغيل (Run)**
```
docker-compose.yml → ينشئ حاويات → الحاويات تعمل
```

### 3. **الربط (Connect)**
```
nginx-app.conf → يربط nginx بـ app
local.ini → يربط PHP بالإعدادات
```

### 4. **الخدمة (Service)**
```
المستخدم → Traefik → nginx → app → Laravel → النتيجة
```

### 5. **التحديث (Update)**
```
GitHub → deploy.yml → السيرفر → git pull → composer install
```

---

## 📝 ملخص العلاقات

| الملف | يستخدم | يُستخدم من قبل |
|-------|--------|----------------|
| `Dockerfile` | - | `docker-compose.yml` |
| `docker-compose.yml` | `Dockerfile`, `nginx-app.conf`, `local.ini` | `deploy.yml`, `git_pull.sh` |
| `nginx-app.conf` | - | `docker-compose.yml` |
| `local.ini` | - | `docker-compose.yml` |
| `git_pull.sh` | `docker-compose.yml` | المطور (يدوياً) |
| `deploy.yml` | `docker-compose.yml` | GitHub (تلقائياً) |

---

## 🎯 الخلاصة

### الملفات الأساسية:
1. **Dockerfile**: وصفة البناء
2. **docker-compose.yml**: مدير الحاويات
3. **nginx-app.conf**: إعدادات الخادم
4. **local.ini**: إعدادات PHP

### الملفات المساعدة:
5. **git_pull.sh**: تحديث يدوي
6. **deploy.yml**: تحديث تلقائي

### كيف يعملون معاً:
```
Dockerfile + docker-compose.yml = بناء وتشغيل
nginx-app.conf + docker-compose.yml = إعداد الخادم
local.ini + docker-compose.yml = إعدادات PHP
deploy.yml = تحديث تلقائي
git_p